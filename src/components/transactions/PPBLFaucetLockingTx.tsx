// Example for Lesson 201.4
// Deposit funds in a treasury
// In this example, we hard-code the values
// Extension #1: Build a form so that user can decide how many tokens to lock
// Extension #2 (Challenge!): Try to build a component that uses your own custom instance of Faucet (as compiled in Module 101)

export default function PPBLFaucetLockingTx() {
  return (
    <div>
      <p>
        In Lesson 100.5, you interacted with the unlocking tx. Here is a
        corresponding Locking tx.
      </p>
    </div>
  );
}
